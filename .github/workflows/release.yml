name: Android CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build Release APK
      run: |
        cd PROFESSOR
        gradle assembleRelease -Pandroid.injected.signing.store.file=$GITHUB_WORKSPACE/PROFESSOR/app/PROFESSOR.jks -Pandroid.injected.signing.store.password=professor2026 -Pandroid.injected.signing.key.alias=professor -Pandroid.injected.signing.key.password=professor2026

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Professor-Release-APK
        path: PROFESSOR/app/build/outputs/apk/release/app-release.apk
